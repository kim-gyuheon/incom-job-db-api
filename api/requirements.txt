fastapi
uvicorn[standard]
anthropic
pydantic
# stt.py의 STT_PROVIDER=openai(기본 운영 방식)가 Whisper 전사 API를 직접 호출한다
# (anthropic이 끌어오지만 명시해 둔다). 2026-08-26: faster-whisper 로컬 경로를 완전히
# 제거하고 openai로 전환했다 — Render 무료 플랜 메모리 한도와 실사용 인식 품질 사이의
# 트레이드오프에서 벗어나기 위한 결정.
httpx
# voice_engine.py: skillmatch-voice-backend의 82축 코사인 유사도 엔진 (engine/, tagging.py,
# vectorizing.py). numpy는 매칭 계산, pandas는 부팅 시 82축/barrier CSV 적재에 쓴다.
numpy
pandas
